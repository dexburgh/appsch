## Packages
date-fns | Date manipulation for theatre times and formatting
recharts | Dashboard analytics charts
framer-motion | Page transitions and animations
lucide-react | Beautiful icons (already in base but explicit check)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
